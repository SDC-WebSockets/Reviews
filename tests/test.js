// function sum(a, b) {
//   return a + b;
// }


// test('adds 1 + 2 to equal 3', () => {
//   expect(sum(1, 2)).toBe(3);
// });


// REVIEWS COLLECTION
// multiples of 13 courseIds should not have reviews or ratings

// all other courses should have 1 to 100 reviews

// a query to the collection should return an array of object with 9 properties each

// each object should have a "reviewer" property, of which the value is an object which 5 properties


// RATINGS COLLECTION
// should have 100 ratings

// multiples of 13 courseIds should not have reviews or ratings

// a query to the collection should return an object with 15 properties

// overall course rating should be an average of all ratings for that course